import React from 'react';
import { Text, View } from 'react-native';

const PostsScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center' }}>
      <Text style={{ textAlign: 'center' }}>PostsScreen</Text>
    </View>
  );
};

export default PostsScreen;
